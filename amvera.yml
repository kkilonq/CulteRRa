meta:
  environment: node
  toolchain:
    name: npm
    version: 20
build:
  pipeline:
    - npm install --prefix frontend
    - npm run build --prefix frontend
run:
  command: npm run start --prefix frontend
